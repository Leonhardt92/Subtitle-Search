# Templates

这个目录放可以提交到 Git 的模板资产。

当前文件：

- [subtitles.template.db](/Users/zhanzz/Subtitle-Search/templates/subtitles.template.db)

用途：

- 作为空表结构数据库模板
- 里面保留一条示例视频和一条示例字幕，方便新环境快速确认表结构和字段含义

说明：

- 当前真实运行库是根目录的 `subtitles.db`
- 真实运行库不再提交到 Git
- 当前真实 `data/幽默/` 和 `data/文笔/` 目录内容也不再提交到 Git
